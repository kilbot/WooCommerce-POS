{
  "name": "backbone-deep-model",
  "version": "2.0.1",
  "description": "Improved support for models with nested attributes.",
  "author": {
    "name": "Charles Davison"
  },
  "scripts": {
    "build": "gulp",
    "test": "./node_modules/mocha/bin/mocha -u bdd",
    "coverage": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage",
    "coverage-html": "istanbul cover ./node_modules/mocha/bin/_mocha -- "
  },
  "dependencies": {
    "backbone": "^1.0.0",
    "jquery": "^2.1.3",
    "lodash.merge": "^3.0.3",
    "underscore": "^1.8.2"
  },
  "devDependencies": {
    "browserify": "^9.0.3",
    "buildify": "latest",
    "chai": "^2.1.1",
    "coveralls": "^2.11.2",
    "gulp": "^3.8.11",
    "gulp-rename": "^1.2.0",
    "gulp-uglify": "^1.1.0",
    "gulp-wrap": "^0.11.0",
    "istanbul": "^0.3.7",
    "mocha": "^2.2.1",
    "mocha-lcov-reporter": "0.0.2",
    "vinyl-buffer": "^1.0.0",
    "vinyl-source-stream": "^1.1.0"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:kahwee/backbone-deep-model.git"
  }
}
